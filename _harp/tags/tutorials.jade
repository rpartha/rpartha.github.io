h1 Tutorials
nav.posts
  ul
    each article, slug in public.writings._data
      if article.title != 'Archive' && article.title != '404' && article.tags.includes('tutorials')
        li
          span.date #{ article.date }
          a(href='/writings/#{ slug }') #{ article.title }
          